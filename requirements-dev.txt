-r scripts/requirements.txt
pytest>=8.0
ruff>=0.6
shellcheck-py>=0.10
# The MCP servers under mcp/ are shipped surface; without the SDK here their
# import test skips, which is how an SDK-2.0 break reached a release unnoticed.
mcp[cli]>=1.2.0,<3
